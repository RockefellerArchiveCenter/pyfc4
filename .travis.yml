language: python
sudo: required
services:
  - docker
before_install:
  - docker pull thomaszastrow/fedora-commons
  - docker run -p 8080:8080 -t -i thomaszastrow/fedora-commons /bin/bash
python:
  - "3.5"
# command to install dependencies
install: "pip3 install -r requirements.txt"
# command to run tests
script: pytest